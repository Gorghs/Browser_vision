/**
 * Content script entry point.
 *
 * Injected into every http(s) page, but inert: interaction listeners are added
 * in a later step and only while the user has tracking switched on. Nothing is
 * observed and nothing is sent from here yet.
 */
export {};
